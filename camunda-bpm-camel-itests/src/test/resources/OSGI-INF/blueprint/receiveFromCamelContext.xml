<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

  <camelContext id="receiveFromCamelContext" xmlns="http://camel.apache.org/schema/blueprint">
    <route id="receiveFromCamelRoute">
      <from uri="direct:receiveFromCamelStartDirect"/>
      <to uri="camunda-bpm://signal?activityId=waitForCamel"/>
      <to uri="mock:receiveFromCamelRouteResultMock"/>
    </route>
  </camelContext>

</blueprint>
